// ─── Overlap Engine ───────────────────────────────────────────────────────────
export const MAX_SECTIONS_SELECTED = 5;
export const OVERLAP_API_BASE = process.env.NEXT_PUBLIC_API_BASE_URL || 'http://localhost:5003';

// ─── Soft Skills Lab ─────────────────────────────────────────────────────────
export const SCORE_MAX = 100;
export const SCORE_GOOD_THRESHOLD = 70;
export const SCORE_OK_THRESHOLD = 50;
export const HISTORY_PAGE_SIZE = 10;

// ─── Code Arena ───────────────────────────────────────────────────────────────
export const WELCOME_BONUS_AP = 1000;
export const DAILY_LOGIN_AP = 10;

// ─── General UI ───────────────────────────────────────────────────────────────
export const DEBOUNCE_DELAY_MS = 300;
export const CLIPBOARD_RESET_DELAY_MS = 2000;
export const TOAST_DURATION_MS = 3000;
